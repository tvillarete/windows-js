const initialState = {
   icons: [
      'StartMenu'
   ]
}

const taskbarReducer = (state = initialState, action) => {
   switch(action.type) {
      default:
         return state;
   }
}

export default taskbarReducer;
