const initialState = {
   background: 'images/bg.jpg'
}

const desktopReducer = (state = initialState, action) => {
   switch(action.type) {
      default:
         return state;
   }
}

export default desktopReducer;
